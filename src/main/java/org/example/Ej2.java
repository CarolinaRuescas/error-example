package org.example;


import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

import java.util.Scanner;

// Escribe un Reader<Student>, el Reader debe pedir los datos
// de un estudiante (nif, name, surname, zipCode) capturando posibles
// excepciones. En caso de que el usuario introduzca algún dato incorreto,
// muestra un mensaje de error y vuelve a pedirlo hasta que lo haga
// correctamente.
public class Ej2 {
    private static final Logger log = LoggerFactory.getLogger(ExceptionsExample.class);
    private final static Scanner scanner = new Scanner(System.in);







}
